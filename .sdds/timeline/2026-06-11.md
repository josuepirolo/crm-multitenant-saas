# Timeline — 2026-06-11

### 09:46 — Admin SaaS: 1 workspace CRM : N `wa_tenant_id` (multi-instância WA)
- Pedido: superadmin precisa vincular várias instâncias WhatsApp (`wa_tenant_id`) a um mesmo workspace CRM (ex.: número "Vendas" + número "Suporte"), mantendo isolamento multi-tenant (1 `wa_tenant_id` → no máx. 1 workspace)
- Migration `supabase/migrations/20260611000000_workspace_integrations_multi_instance.sql` aplicada via `apply_migration` (projeto `gkzqhlaltnlcpzcapayb`): remove `UNIQUE (workspace_id, integration_type, provider_id)`, adiciona coluna `label TEXT`, adiciona `UNIQUE (wa_tenant_id)` — Postgres trata múltiplos `NULL` como distintos, então vínculos "pending" sem `wa_tenant_id` continuam permitidos
- Tipos novos em `src/types/index.ts`: `IntegrationStatus`, `WorkspaceIntegration`, `WorkspaceIntegrationWithWaTenant`, `WaTenantOption`, `WaProviderOption`
- `SupabaseWorkspaceIntegrationRepository` (`src/repositories/workspace-integration.repository.ts`) — leitura cross-domain somente-leitura de `wa_tenants`/`wa_instances`/`wa_providers` (reforça ADR-001, nunca escreve em `wa_*`)
- 6 use cases em `src/usecases/WorkspaceIntegrationUseCases.ts`; validação Zod em `src/lib/validations/workspace-integration.ts`
- Server Actions `src/app/(admin)/admin/integrations-actions.ts` — todas atrás de `requireSuperAdmin()`, `createAdminClient()`, auditoria via `AUDIT_ACTIONS.INTEGRATION_LINKED/UPDATED/UNLINKED` (novas constantes em `src/lib/audit/audit-log.ts`); erro de unique violation em `wa_tenant_id` vira mensagem genérica "Esta instância já está vinculada a outra empresa."
- ViewModel `useWorkspaceIntegrationsViewModel` (toast.promise + atualização otimista com rollback)
- UI `WorkspaceIntegrationsSection` (accordion) integrada ao `WorkspaceDetailPanel` via `<WorkspaceIntegrationsSection key={workspace.id} workspaceId={workspace.id} />` — o `key` força remount ao trocar de workspace, evitando `useEffect` de reset (`react-hooks/set-state-in-effect`)
- ADR-005 criado: `decisions/ADR-005-admin-wa-tenant-mapping.md`
- Verificação: `tsc --noEmit` limpo; `npx eslint` limpo no código novo (resta só 1 erro pré-existente em `workspace-detail-panel.tsx:65`, não tocado nesta sessão); `vitest run` → 384/384
- Pendente: commit; teste manual em `/admin/workspaces` (vincular/editar/remover instância, dark mode, mobile 375px)
- Sessão: `.sdds/sessions/2026-06-11-0635-session.md`
