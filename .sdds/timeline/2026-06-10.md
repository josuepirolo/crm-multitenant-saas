# Timeline — 2026-06-10

### 03:27 — Segunda metade do fix de impersonação: `getScopedSupabaseClient()` + "Criar nova origem" sempre disponível
- Causa raiz: `getWorkspaceContext`/`getCurrentWorkspaceId` resolviam `workspaceId = <impersonado>` (ADR-004), mas Server Actions de `contacts/` continuavam com `createClient()` RLS-bound ao superadmin — `my_workspace_ids()` só retorna PyTec, então leituras/escritas em Lekazis eram bloqueadas pelo RLS mesmo com `workspaceId` correto
- Sintomas: `ContactSourcesSheet` não mostrava as 11 origens da Lekazis (já remediadas no banco); reimportação da planilha Lekazis ficaria bloqueada por `WITH CHECK`
- Novo `getScopedSupabaseClient()` em `src/lib/guards.ts`: retorna `createAdminClient()` (service_role) durante impersonação validada, senão `createClient()` (RLS-bound, sem mudança para fluxo normal) — drop-in `Promise<SupabaseClient>`
- Aplicado em `contacts/{actions,import-actions,niche-profile-actions}.ts` (18 call sites)
- `ImportContactsDialog`: bloco "Criar nova origem" extraído para `CreateSourceInline`, removido de dentro do alerta âmbar de coluna ausente, agora sempre visível nas etapas `preview` (independente de `isSubmitting`/`hasOriginColumn`) e `result`
- 3 novos testes para `getScopedSupabaseClient` em `workspace-context-impersonation.test.ts`; `actions-auth.test.ts` ajustado para mockar o helper
- `tsc --noEmit` limpo; `vitest run src/tests/security src/tests/tenant-isolation` → 326/326 (era 323)
- R-009 (ALTO, ABERTO) registrado: ~15 arquivos fora de `contacts/` com o mesmo padrão `createClient()` pós-`getWorkspaceContext`, ainda não corrigidos
- Discovery: `discoveries/2026-06-10-impersonation-rls-blocks-data-access.md`; addendum em `decisions/ADR-004-impersonation-owner-like-access.md`
- Sessão: `.sdds/sessions/2026-06-10-0327-session.md`
- Commit `df33790` (fix anterior commitado nesta data)

### ~00:50 — R-009 resolvido: `getScopedSupabaseClient()` aplicado nos 16 arquivos restantes
- Gatilho: usuário reportou dashboard mostrando 0 contatos ao impersonar Lekazis — mesmo padrão do bug acima, em `dashboard/page.tsx`
- Aplicado `createClient()` → `getScopedSupabaseClient()` em: `dashboard/page.tsx`, `kanban/actions.ts` (8), `settings/{actions,niche-actions,rbac-actions,upload-actions}.ts` (6+2+6+1), `auto-parts/{actions,page,quotes/page}.tsx`, `auto-sales/{actions,page,proposals/page}.tsx`, `fashion/{actions,page,stock/page}.tsx`
- `settings/upload-actions.ts`: só `uploadWorkspaceLogo` mudou — `uploadUserAvatar` mantém `createClient()` (escopo do próprio usuário, não do workspace)
- `area-tracking-actions.ts`: sem mudança (não usa `createClient()`)
- 3 testes (`settings-actions`, `upload-actions`, `workspace-profile-actions`) precisaram adicionar `getScopedSupabaseClient` ao mock de `@/lib/guards`
- `tsc --noEmit` limpo; `vitest run` completo → 384/384
- R-009 → RESOLVIDO 2026-06-10
- Pendente: commit + validação manual (dashboard, kanban, settings, auto-parts, auto-sales, fashion sob impersonação)

### ~01:00 — Guardrail ESLint para R-009
- Nova regra `no-restricted-imports` em `eslint.config.mjs` (`src/app/(dashboard)/**/*.{ts,tsx}`): bane `createClient` de `@/lib/supabase/server`, mensagem aponta para `getScopedSupabaseClient()`
- Regra revelou +5 arquivos com `createClient()` legítimo (escopo `auth.uid()`/catálogo global, não `workspace_id`): `(dashboard)/actions.ts`, `(dashboard)/layout.tsx`, `(dashboard)/session-actions.ts`, `settings/mfa-actions.ts`, `setup-niche-action.ts` — cada um com `eslint-disable-next-line` + justificativa
- `kanban/actions.ts` e `settings/rbac-actions.ts`: `createClient` usado só como tipo → trocado por `SupabaseClient` de `@supabase/supabase-js`, import removido
- `npx eslint "src/app/(dashboard)/**/*.{ts,tsx}"` → 0 erros `no-restricted-imports` (restam 3 `no-explicit-any` + 2 `no-unused-vars` pré-existentes); `tsc --noEmit` limpo; `vitest run` → 384/384
- Pendente: commit (R-009 + guardrail)
