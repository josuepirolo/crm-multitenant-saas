# Timeline — 2026-06-15

## Bug-fixes da integração WhatsApp + discovery do `/qrcode`

- **QR de pareamento corrigido** (`ccdd920`): `/qrcode` devolve `{value: URL de pareamento}`, não
  `{qrcode: data-URI}` (descasamento de contrato). CRM passou a ler `value` e renderizar o QR
  **localmente** com `QRCodeSVG` (`qrcode.react`), sem serviço externo. Suíte 418/418.
- **Discovery** `discoveries/2026-06-15-qrcode-contract-mismatch.md` (`790afa9`) + recado entregue ao
  time do backend WA (corrigir o doc do contrato).
- Antes (mesma sessão, 2026-06-14→15): fix RLS UPDATE workspaces owner+admin (`da21a30`); v2.3a (`36b6c10`)
  e v2.3b (`cc3ffba`) perfil/privacidade/foto; fix "Não definido" para perfil/privacidade não-sincronizados
  (`99cfbc3`, limitação confirmada: Z-API não expõe leitura → GET retorna tudo null).
- **Backend WA respondeu o recado:** atualizou `backend_zapi/frontend/*.md` + novo bundle `frontend_v3/`
  — **revisar na próxima sessão** (ver se `/qrcode` foi documentado e se há mudanças que afetam o CRM).
- `origin/dev` sincronizado (HEAD `790afa9`). Detalhe em `sessions/2026-06-15-0649-session.md`.
