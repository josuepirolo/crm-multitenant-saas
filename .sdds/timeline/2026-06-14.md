# Timeline — 2026-06-14

## Authz cross-service (ADR-007) — hook ativo em produção, mas passo 4 bloqueado no lado WA

- **Custom Access Token Hook HABILITADO** no Supabase (Auth → Hooks → Custom Access Token →
  `public.custom_access_token_hook`) e **confirmado ao vivo**: token real do superadmin `jdredes` sai
  com o claim `authz` correto (`v:1, superadmin:true, PyTec→owner→3 perms`). Também provado via usuário
  descartável (`HOOK_LIVE: SIM`). Grants OK; login não afetado (o 400 visto foi `captcha_failed` do
  Turnstile no password-grant, não o hook).
- **BLOQUEADOR descoberto (lado WA):** backend WA responde `401 {"detail":"Token inválido ou expirado"}`
  a tokens válidos. Tokens são **ES256** (kid `b88e0f82`, publicado no JWKS), `iss`/`aud=authenticated`
  corretos — o `require_auth` do WA não aceita ES256 (provável HS256 legado / cache JWKS stale).
  **Nada a corrigir no CRM.** Bloqueia dados reais da UI de Integrações (T1) e o passo 4 (WA Fase 1).
  Registrado em `discoveries/2026-06-14-wa-backend-rejects-es256-jwt.md`; handoff repassado ao backend.
- Verificações feitas via fluxo admin `generate_link`→`verify` (sem captcha); usuários descartáveis
  criados/apagados com **resíduo zero** (conferido em `auth.users` e `profiles`).
- **RESOLVIDO (tarde 2026-06-14):** backend WA passou a aceitar JWT ES256 via JWKS. Confirmado fim-a-fim:
  `jdredes` como manager do Lekazis → claim com `connection:view` → `GET .../instances` **200** (instância
  "Lekazis Disparos", connected). Ver addendum em `discoveries/2026-06-14-wa-backend-rejects-es256-jwt.md`.
- **Fixes CRM (2026-06-14):** `signOut`/`signIn` agora chamam `clearImpersonation()` (cookies `imp-*` não
  sobrevivem ao logout); UI de `/settings` trata impersonação como `owner` para botões de gestão.
- Commits ADR-006/007 na branch `dev` — push pendente; fixes desta sessão (impersonação + e2e `--admin`) ainda **não commitados**.

## Fixes de impersonação e tooling e2e (noite, ~19:23)

- **`clearImpersonation()`** em `signOut()` e `signIn()` — logout/login não preserva cookies `imp-*`.
- **`/settings`:** `isImpersonating` no BFF + UI trata como `owner` (botão Convidar durante impersonação).
- **`scripts/authz-e2e.mjs --admin`** — validação de claim sem Turnstile; checklist §5.4 atualizado.
- Testes: `sign-out-actions.test.ts` (+1); `auth-actions.test.ts` mock atualizado.
- Ver detalhes: `sessions/2026-06-14-1923-session.md`.

## Commits e validações (~19:51)

- **3 commits** criados e alinhados à sessão 1923: `feb63dc` (impersonação) / `0a9fc92` (e2e `--admin`) / `27391f1` (SDDS).
- Branch `dev` com **6 commits** à frente de `origin/dev` — push pendente.
- Usuário confirmou: logout não mantém impersonação; `authz-e2e.mjs --admin` ✅ (manager + HTTP 200 no WA).
- Login bloqueado temporariamente por rate limit (5/15min) — UI Integrações ainda não validada no browser.
- Ver: `sessions/2026-06-14-1951-session.md`.

## Push concluído (~22:11)

- `git push origin dev` — 7 commits em `origin/dev` (HEAD `578ef09`).
- Branch local limpa; pendências: validação UI Integrações + e2e §5.4 por papel.
- Ver: `sessions/2026-06-14-2211-session.md`.
