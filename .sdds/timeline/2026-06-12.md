# Timeline — 2026-06-12

### 05:43 — Commit da feature WA admin (ADR-005) + remoção de hooks SDDS
- Commit `b699270` feat(admin): mapeamento 1 workspace : N `wa_tenant_id` (ADR-005) — consolida o trabalho da sessão 2026-06-11 (migration `20260611000000_workspace_integrations_multi_instance.sql`, repository/usecases/actions/viewmodel/UI em `/admin/workspaces`, novas constantes `AUDIT_ACTIONS.INTEGRATION_*`, tipos em `src/types/index.ts`), docs SDDS (ADR-005, sessions, timeline) e README atualizado
- Guardrail `pre-commit` (`_sdds_private/githooks/pre-commit`) exigiu README.md staged por alterar arquivos de produto — adicionada menção ao mapeamento WA na seção "Estado atual do produto" do README
- Commit `f467ed8` chore(sdds): removidos os hooks `check-file-size.js`, `validate-spec.js` e `update-index.js` de `pre`/`post-tool-use` em `.claude/settings.json` e `.cursor/hooks.json` — `enforce-guardrails.js` permanece ativo (tool-use e pre-commit); commitado com `--no-verify` por ser mudança de tooling sem impacto documentável no README (decisão do usuário)
- Commit `116eaf3` chore(sdds): atualiza `files.index.md` pós-commit
- Sessão: `.sdds/sessions/2026-06-11-2336-session.md`

### 06:00 — R-007 camada 1: auditoria de DDL em produção (event triggers)
- Migration `20260612090000_ddl_audit_log_event_triggers.sql` criada, commitada e aplicada via `apply_migration` (projeto `gkzqhlaltnlcpzcapayb`)
- `ddl_audit_log` append-only (RLS SELECT só superadmin, mesmo padrão de `audit_logs`; REVOKE escrita anon/authenticated) + event triggers `ddl_audit_command_end`/`ddl_audit_sql_drop` com funções `SECURITY DEFINER` exception-safe
- Autoverificação passou: `COMMENT` final da migration registrado como `id=1`; triggers `enabled='O'` confirmados em `pg_event_trigger`
- R-007: ABERTO → MITIGADO (camada 2 — drift check `supabase db diff` em CI — pendente)
- R-003 (Vault): decidido adiar — sem secrets do CRM para guardar hoje (credenciais WA vivem no backend WA, ADR-001); resolver junto com R-002 + tela `/settings/integrations`

### 06:20 — Documentação do fix R-007
- `docs/security/ddl-audit.md` criado (commit `950af2a`): contexto do incidente, componentes, modelo append-only, queries de investigação, verificação pós-deploy, limitações/camada 2
- README: nova linha na tabela de documentação apontando para o doc

### 06:32 — Prompts de frontend unificados
- Análise comparativa dos prompts `01`/`02` de `specs/PROMPTS_FRONTEND/` (produto vs conversão): fortes em conjunto (~80%), mas sem roteador de modo, sem padrões data-heavy, sem SEO e com risco de dark patterns no 02
- Criado `03_UNIFIED_FRONTEND_MASTER.md` (commit `3cfd3fa`): Passo 0 de detecção de modo (PRODUTO/CONVERSÃO/HÍBRIDO) com regras de fronteira, padrões data-heavy (tabelas com estado na URL, forms multi-step, permissões esconder-vs-desabilitar), seção SEO/JSON-LD, cláusula de honestidade (prova social/escassez só com dados reais) e precedência do design system do projeto sobre o prompt
- Sessão: `.sdds/sessions/2026-06-12-0632-session.md`
