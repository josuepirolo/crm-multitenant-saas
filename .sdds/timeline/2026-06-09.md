# Timeline — 2026-06-09

### 08:05 — Feedback de importação, multi-origem de contatos e correção de bug crítico de impersonação
- Importação: novos contadores `already_exists`/`invalid_count`/`file_duplicates` em `ContactImportResult`; UI (`ImportResultSummary`) com 4 cards e mensagens dedicadas para "tudo já existe"
- Multi-origem por contato: tabela `contact_source_assignments` (N:N, RLS), migration `20260609120000_contact_source_assignments.sql` aplicada via MCP migrando 8.485 `source_id` existentes; `resolveSourceIds` separa origens por vírgula na importação — ver ADR-003
- **Bug crítico de segurança**: `getWorkspaceContext`/`getCurrentWorkspaceId`/`getUserRole` ignoravam o cookie de impersonação e gravavam no workspace do superadmin durante impersonação — 8.485 contatos foram parar em PyTec ao importar para Lekazis. Corrigido com `getValidatedImpersonatedWorkspaceId` (acesso owner-like ao workspace impersonado — ADR-004); 8.485 contatos removidos de PyTec (workspace de teste). `tsc` limpo, 307/307 testes de segurança/tenant-isolation passando
- Pendente: testes automatizados específicos de impersonação; reimportação da planilha Lekazis pelo usuário
- Sessão: `.sdds/sessions/2026-06-09-0805-session.md`; discovery: `discoveries/2026-06-09-impersonation-tenant-isolation-bug.md`

### 08:50 — Testes automatizados do fix de impersonação (R-008 resolvido)
- Novos testes: `src/tests/security/impersonation-validated-workspace.test.ts` (5 testes de `getValidatedImpersonatedWorkspaceId`) e `src/tests/security/workspace-context-impersonation.test.ts` (11 testes de `getCurrentWorkspaceId`/`getWorkspaceContext`/`getUserRole`, com e sem impersonação)
- `vitest run src/tests/security src/tests/tenant-isolation` → 323/323 passando (era 307, +16 novos); `tsc --noEmit` limpo
- R-008 (risco ALTO) → RESOLVIDO; pendente: usuário reimportar planilha Lekazis; commit das alterações da sessão
- Sessão: `.sdds/sessions/2026-06-09-0805-session.md`

### 07:45 — Dashboard: deals ocultos, card Contatos por Estado adicionado
- Seção de negociações removida do dashboard (DealsChart + 2 cards); módulo em validação de produto
- Novo `ContactsByStateCard`: top N estados por DDD, barras proporcionais, rodapé sem-telefone
- RPC `get_contacts_by_state(uuid)` via migration: agrega no banco, resolve limit 1000 do PostgREST
- `DashboardStats` simplificado; card renomeado para "Total de Contatos"
- Sessão: `.sdds/sessions/2026-06-09-0723-session.md`

### 07:30 — Normalização de nomes: title case PT-BR no parser e em lote no banco
- `toTitleCase` em `parse-contact-import.ts`: preposições PT (`de da das do dos e em na nas no nos`) minúsculas, trim, colapso de espaços duplos
- Banco: 4.324/8.486 contatos normalizados via função PL/pgSQL temporária `_normalize_name` + UPDATE; 0 restantes fora do padrão após execução
- Sessão: `.sdds/sessions/2026-06-09-0659-session.md`

### 06:50 — Normalização de telefone brasileiro implementada (`normalizeBrazilianPhone`)
- Função `normalizeBrazilianPhone` em `src/lib/validations/contact.ts`: detecta comprimento de dígitos para determinar DDI/DDD, injeta nono dígito apenas em celulares (iniciam 6-9 pós-DDD com 8 dígitos)
- Trata ambiguidade DDI 55 vs DDD 55 (Três Lagoas/MS) pelo comprimento total dos dígitos
- Aplicada nos dois pontos de entrada: importação planilha (`parse-contact-import.ts`) e formulário manual (`actions.ts`)
- `normalizePhone` passa a delegar para `normalizeBrazilianPhone` — compatível com E.164 do modal (`react-phone-number-input`)
- tsc limpo, 365/365 passando
- Sessão: `.sdds/sessions/2026-06-09-0650-session.md`

### 00:01 — Importação em lotes automáticos implementada
- `parseContactImportFile` agora aceita `offset`/`limit` — sem limite de linhas, arquivos grandes divididos em lotes de 2.000 automaticamente
- `importContactsAction` orquestra lotes via FormData (`offset`, `chunk_size`); rate limit + audit log apenas no lote 0
- `useContactImportViewModel` executa loop de N lotes, agrega resultados, expõe progresso (`currentChunk`, `totalChunks`, `processedRows`)
- `ImportContactsDialog` exibe barra de progresso animada + contador "N/total linhas" durante importação em lotes
- `totalDataRows` adicionado ao `ImportPreviewResult` — base para calcular número de lotes e exibir progresso
- Sessão: `.sdds/sessions/2026-06-09-0001-session.md`
