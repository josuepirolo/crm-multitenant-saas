# Timeline — 2026-06-16

## 06:03 — Gerenciar grupo + mapWaError contextualizado (commit `020461d`)

**Contexto:** sessão de continuidade — ADR-008 fases 2-4 tinham sido commitadas, mas dois problemas persistiam: erros silenciosos/genéricos do `mapWaError` e ausência de UI de gerenciamento de grupo (ViewModel com handlers, sem componente que os chamasse).

**Implementado:**
- `mapWaError` agora recebe `context?: "group" | "campaign"` — switch por HTTP status (400/403/409/422) com mensagens específicas por contexto
- `WaManageGroupSheet` — painel lateral (slide-right) com renomear, descrição, add/remove participantes; usa sempre `group_provider_id` (ID Z-API §4)
- Botão `Settings2` por card de grupo; `getGroupMetadata` (§4 GET) em todos os layers (repository→usecase→action→viewmodel)
- 418/418 · tsc limpo
