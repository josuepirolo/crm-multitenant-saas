---
session: 2026-06-15-2305
evento: ADR-008 fases 2-4 — console operacional WhatsApp (grupos, enviar, campanhas)
commits: 5879b74 (fase 1), b63303e (fases 2-4)
testes: 418/418 passando
tsc: 0 erros
---

## O que foi feito

### Fase 1 (commit 5879b74)
- Sidebar: menu WhatsApp (visível só com `workspace_integrations` whatsapp)
- Rota `/whatsapp/conexao` — reusa UI v2.3 (`IntegrationsTab`)
- `/settings/integrations` ganha banner → `/whatsapp/conexao`
- Harness `whatsapp-console.harness.md` (WC-01..14 fase 1; WC-20..43 fases 2-4 pendentes)

### Fases 2-4 (commit b63303e — 21 arquivos, +2391 linhas)
- **Tipos** (`src/types/index.ts`): WaConversation, WaGroupReadiness, WaSendMessageBody, WaCampaign + ciclo de vida
- **Audit** (`audit-log.ts`): 7 novas ações WA_GROUP_*/WA_MESSAGE_SENT/WA_CAMPAIGN_*
- **Repository** `wa-operational.repository.ts`: IWaOperationalRepository + WaBackendOperationalRepository — §4 groups, §5 messages, §6 conversations (is_group=true), §7 campaigns
- **15 use cases** (`WaOperationalUseCases.ts`)
- **Helper** `whatsapp/_helpers.ts`: `authorizeWaOperation` — UUID validation + RBAC gate + tenantId anti-IDOR
- **Server actions**: grupos/ (listar, criar, renomear, desc, participantes), enviar/ (sendWaMessage, Zod, sem log de conteúdo), campanhas/ (CRUD + ciclo de vida)
- **ViewModels**: useWaGruposViewModel, useWaEnviarViewModel, useWaCampanhasViewModel
- **Componentes**: WaInstanceSelector, WaGruposClient, WaEnviarForm, WaCampanhasClient + dialogs (grupos, campanhas)

## Decisões técnicas da sessão

- Dialogs com `AnimatePresence` + `ModalOverlay` + `createPortal` (padrão nativo — não existe shadcn Dialog)
- Native `<textarea>` (não existe `@/components/ui/textarea`)
- DropdownMenu substituído por botões inline condicionais por status (não existe `@/components/ui/dropdown-menu`)
- Select `onValueChange` precisa de guard `v !== null` — base-ui passa `string | null`
- Grupos: listados via `/conversations?is_group=true` (não tem endpoint /groups separado)
- Campanha `audience_type: "filter"` com `phones[]` — não `audience_type: "contacts"` (não existe)
- `WA_MESSAGE_SENT` nunca loga conteúdo da mensagem (privacidade, ADR-001)
- Página usa Server Component para buscar `listWorkspaceWaInstances()`, passa `instances` para Client Component

## Riscos / pendências

- Validação manual no navegador: grupos (listar/criar), enviar mensagem, campanhas (criar/disparar)
- Harness WC-20..43: marcado como PENDENTE (validar manualmente quando backend estiver disponível)
- `wa-instance-selector.tsx`: retorna `null` se `instances.length <= 1` — multi-instância não exercitado ainda
- Working tree: `.sdds/indexes/files.index.md` e `backend_zapi/frontend/README.md` ainda não commitados
