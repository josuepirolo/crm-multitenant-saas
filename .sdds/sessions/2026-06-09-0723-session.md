# Sessão — 2026-06-09 07:23 (local)

## Metadados
- **Data:** 2026-06-09
- **Hora:** 07:23 (local)
- **Branch:** dev

## O que foi feito

- **Dashboard — seção Negociações removida**: `DealsChart`, cards "Negociações em aberto" e "Taxa de conversão" ocultados. Módulo de deals ainda em validação de produto — código preservado (`deals-chart.tsx` autocontido, sem dependência de repositório). `DashboardStats` simplificado: removidos `openDealsValue`, `openDealsCount`, `wonDealsCount`, `totalDealsCount`. Card renomeado para "Total de Contatos".
- **Novo card "Contatos por Estado"**: lista ranqueada top 6 estados com barra proporcional, subtitle dinâmico "Top N estados · distribuição por DDD", rodapé "X contatos sem telefone". Componente `contacts-by-state-card.tsx`.
- **RPC `get_contacts_by_state(uuid)`** — migration `20260609_get_contacts_by_state_rpc.sql`: agrega no banco via JOIN com tabela VALUES de DDDs (80 DDDs → 27 estados), retorna `(state, cnt, without_phone)`. Resolve limit padrão de 1000 linhas do PostgREST — dados ficam no banco, TypeScript recebe só 6 linhas.
- **Repositório**: `getContactsByState` agora usa `.rpc()`. Mapa `DDD_TO_STATE` removido do TypeScript.

## Decisões tomadas

- Aggregação DDD→Estado feita em SQL (não TypeScript): mais eficiente, sem limite de rows, sem transferência de dados brutos.
- Deals ocultos mas não deletados — facilita reativar quando módulo for validado.

## Próximos passos

- Teste manual do dashboard no navegador
- Spec módulo WA Integrations
