/**
 * Auto-cadastro (página /register) desativado temporariamente por decisão de produto.
 * A página e o formulário continuam no código — apenas o acesso é bloqueado.
 * Reverter para `true` quando o cadastro público for reabilitado.
 */
export const SELF_REGISTRATION_ENABLED = false;
